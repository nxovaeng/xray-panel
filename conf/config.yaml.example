# Xray Panel 配置文件示例
# 复制此文件为 config.yaml 并按实际情况修改

server:
  listen: "127.0.0.1:8082"   # 建议只监听本地，通过 Nginx 反代
  debug: false

log:
  level: "info"               # debug / info / warning / error
  file: "/opt/xray-panel/logs/panel.log"
  max_size: 100               # 单文件最大 MB
  max_backups: 7
  max_age: 30                 # 旧日志保留天数
  compress: true

database:
  path: "/opt/xray-panel/data/panel.db"

jwt:
  secret: "change-me-please-use-random-string-min-32-chars"
  expire_hour: 168            # Token 有效期（小时），默认 7 天

admin:
  username: ""                # 留空则首次启动自动生成
  password: ""                # 留空则首次启动自动生成强密码
  email: ""

xray:
  binary_path: "/usr/local/bin/xray"
  config_path: "/usr/local/etc/xray/config.json"
  assets_path: "/usr/local/share/xray"   # geoip.dat / geosite.dat
  api_port: 10085
  socket_dir: "/dev/shm"     # UDS 目录，/dev/shm 性能更好

nginx:
  config_dir: "/etc/nginx/conf.d"
  stream_dir: "/etc/nginx/stream.d"      # SNI 路由
  reload_cmd: "systemctl reload nginx"
  cert_dir: "/root/.acme.sh"
